# Opt-in Schedule patch over the shipped Web composition. The owner observes
# only roots published after this overlay loads.

- insert:
    - id: time-context
      name: '@solsticeai/equinox-time-context'

    - id: schedule
      name: '@solsticeai/equinox-schedule'

- id: ui-schedule
  disabled: false
